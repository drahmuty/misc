#include <stdio.h>

int findmatch(char *, char *);

int main(void)
{
    // put something here
}

int findmatch(char *p, char*t)
{
    char plen;
    char *pp, *tp;
    
    pp = p;
    tp = t;
    while (*pp++)
        ;
    plen = pp-p;
    for ( ; t < plen)
        ;
    
}
